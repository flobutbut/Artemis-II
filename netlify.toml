[build]
  command = "npm run build"
  publish = "dist"

# Proxy sans CORS vers l’API Horizons (même chemin que Vite en dev).
[[redirects]]
  from = "/jpl-horizons"
  to = "https://ssd.jpl.nasa.gov/api/horizons.api"
  status = 200
  force = true
